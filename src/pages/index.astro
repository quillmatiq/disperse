---
import Base from "../layouts/Base.astro";
---
<Base>
  <script>
    import { getActiveSession } from "../lib/auth";
    const active = await getActiveSession();
    window.location.replace(active ? "/share" : "/login");
  </script>
</Base>
